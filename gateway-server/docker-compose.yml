version: '3.8'

services:
  redis:
    image: redis:7
    container_name: redis
    command: redis-server --requirepass qwer --appendonly yes
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis-data:/data

volumes:
  redis-data:
